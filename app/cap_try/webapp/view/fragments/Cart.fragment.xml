<core:FragmentDefinition
	xmlns="sap.m"
	xmlns:core="sap.ui.core"
    xmlns:table="sap.ui.table"
>
    <Dialog title="Shopping Cart" id="Cart">
        <content>
            <table:Table
                id="cartTable"
                visibleRowCount="5"
                selectionMode="MultiToggle"
                alternateRowColors="true"
                class="sapUiSmallMargin">
                <table:extension>
                    <OverflowToolbar>
                            <Button text="Add Cart" iconFirst="true" icon="sap-icon://add" press="onAddCartButtonPress" />
                        <ToolbarSeparator />
                            <Label text="Cart's" design="Bold" />
                            <Select autoAdjustWidth="false" columnRatio="3:2" forceSelection="false" id="cartsSelect"
                                    icon="sap-icon://cart" maxWidth="100%"  items="{ path: '/Cart',
                                                                                     sorter: { path: 'createdAt', descending: false } }" 
                                    change="onCartsSelectChange" resetOnMissingKey="false" width="10%" wrapItemsText="false"
                                    enabled="true" >
                                <items>
                                    <core:Item key="{ID}" text="{name}" />
                                </items>
                            </Select>
                            <Button
                                icon="sap-icon://delete"
                                type="Transparent"
                                tooltip="{i18n>delete_current_cart}"
                                class="sapUiTinyMarginBegin"
                                press="onDeleteSelectedCartPress"
                                enabled="true" />
                        <ToolbarSpacer />
                        <ToolbarSpacer />
                            <Title level="H2" text="Total" titleStyle="Auto" width="" wrapping="false" wrappingType="Normal" />
                            <Text text="{= ${globalModel>/products}.length }" />
                    </OverflowToolbar>
                </table:extension>
                <table:columns>
                    <table:Column width="20%">
                        <Label text="Name" />
                        <table:template>
                            <Text text="{product/name}" />
                        </table:template>
                    </table:Column>
                    <table:Column width="20%">
                        <Label text="Description" />
                        <table:template>
                            <Text text="{product/description}" />
                        </table:template>
                    </table:Column>
                    <table:Column width="20%">
                        <Label text="Price" />
                        <table:template>
                            <ObjectNumber number="{product/price}"
                                          unit="{globalModel>/selectedCompany/currency}" />
                        </table:template>
                    </table:Column>
                    <table:Column width="20%">
                        <Label text="Quantity" />
                        <table:template>
                            <StepInput change="onProductCartQuantityChangePress" value="{quantity}" />
                        </table:template>
                    </table:Column>
                    <table:Column width="20%">
                        <Label text="Total" />
                        <table:template>
                            <!-- <Text text="{= ${globalModel>quantity} * ${globalModel>price}}" /> -->
                            <ObjectNumber number="{total_price}"
                                          unit="{globalModel>/selectedCompany/currency}" />
                        </table:template>
                    </table:Column>
                    <table:Column width="5rem">
                        <Label text="{i18n>delete}" />
                        <table:template >
                            <Button
                                icon="sap-icon://delete"
                                type="Transparent"
                                tooltip="{i18n>delete_cart_product}"
                                press="onDeleteProductPress" />
                        </table:template>
                    </table:Column>
                </table:columns>
                <table:footer>
                    <OverflowToolbar>
                        <Button icon="sap-icon://delete" text="{i18n>delete_products}" press="onDeleteCartProductPress" />
                        <ToolbarSeparator visible="{= ${globalModel>/cart}.length === 0 ? false : true}" />
                        <Button text="{i18n>finalize_purchase}" visible="{= ${globalModel>/cart}.length === 0 ? false : true}" press="onFinalizePurchaseButtonPress" type="Success" />
                        <ToolbarSpacer />
                        <Label text="Total do Carrinho:" design="Bold" />
                        <Text class="sapUiTinyMarginBegin" text="{
                                parts: [ 
                                    {path: 'globalModel>/selectedCart/total_price'},
                                    {path: 'globalModel>/selectedCompany/currency'}
                                ],
                                formatter: '.formatter.priceCurrencyFormatter'
                            } {globalModel>/selectedCompany/currency}" />
                    </OverflowToolbar>
                </table:footer>
            </table:Table>
        </content>
        <beginButton>
            <Button text="{i18n>close}" press="closeCartDialog" />
        </beginButton>
    </Dialog>
</core:FragmentDefinition>